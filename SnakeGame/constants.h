#pragma once

const int SIZE_OF_SNAKE_AND_FOOD_SEGMENT = 15;
const int GAME_WINDOW_WIDTH_IN_SEGMENTS = 35;
const int GAME_WINDOW_HEIGHT_IN_SEGMENTS = 25;
const sf::Color BACKGROUND_COLOR = sf::Color(116, 157, 161);
const sf::Color SNAKE_COLOR = sf::Color(180, 190, 137);
const sf::Color FOOD_COLOR = sf::Color(235, 145, 145);
//Two more colors in this colors palette if needed in the future: (224,190,153) (like sand), (66, 76, 81) (nice grey)
const float TIME_BETWEEN_SNAKE_MOVEMENTS = 0.5f;